# Patched files in node_modules

- `context-mode/build/adapters/pi/extension.js` - local patch applied to context-mode (version at sync time: 1.0.169).

Any `npm update` of context-mode overwrites this file: copy it back from here after each update (or via `uv run scripts/restore.py --apply`).
