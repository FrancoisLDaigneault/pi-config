# Fichiers patches dans node_modules

- `context-mode/build/adapters/pi/extension.js` — patch local applique a context-mode (version au moment du sync : 1.0.169).

Tout `npm update` de context-mode ecrase ce fichier : le recopier depuis ici apres chaque update (ou via `uv run scripts/restore.py --apply`).
