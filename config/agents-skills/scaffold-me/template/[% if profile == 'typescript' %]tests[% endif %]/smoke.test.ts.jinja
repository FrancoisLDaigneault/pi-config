import { describe, expect, it } from "vitest";

import { version } from "../src/index.js";

describe("package", () => {
  it("is importable", () => {
    expect(version).toBe("0.1.0"); // x-release-please-version
  });
});
