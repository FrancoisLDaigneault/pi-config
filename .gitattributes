# Normalize all text files to LF in the repository and the working tree.
# Ends the recurring CRLF conversion noise on Windows checkouts; the shell
# hook (hooks/pre-commit) requires LF to run.
* text=auto eol=lf
